# Приложение для интернет-магазина техники.

Домашняя работа по четвертому модулю обучения.
---
## Установка
1. Клонирование репозитория
```
git clone https://github.com/ann-novikova/Skypro_project4.git
```
2. Установка зависимостей
```
poetry install
```
---
## Основной функционал

Главная функция `main` принимает на вход аргументы для создания объектов из классов Product и Category.

## Описание модуля `product`

Создан класс Product и Category.

Для класса Product определены следующие свойства:

* название (name),
* описание (description),
* цена (price),
* количество в наличии (quantity).

Для класса Product есть строковое отображение в следующем виде:
*Название продукта, 80 руб. Остаток: 15 шт.*

Для удобства работы с продуктами реализована возможность их складывать. 
Логика сложения работает так, чтобы в итоге у вас получалась полная стоимость всех товаров на складе.
Например, для товара a с ценой 100 рублей и количеством на складе 10 и товара b с ценой 200 рублей и количеством 
на складе 2 результатом выполнения операции a + b должно стать значение, полученное из 100 × 10 + 200 × 2 = 1400.

Для класса Product создан класс-метод new_product, который принимает на вход параметры товара в словаре и 
возвращает созданный объект класса Product. Для данного метода реализована проверка наличия такого же товара схожего 
по имени. В случае если товар уже существует, считается общее количество в наличии старого товара и нового. 
При конфликте цен выбирается та, которая является более высокой.

Для класса Product атрибут цены является приватным, поэтому реализованы геттеры и сеттеры. 
В сеттере есть проверка: в случае если цена равна или ниже нуля возникает ошибка:
“Цена не должна быть нулевая или отрицательная”, при этом новая цена не устанавливается.
В случае если цена товара понижается, есть дополнительная опция подтверждения пользователем вручную через ввод 
y (значит yes) или n (значит no) для согласия понизить цену или для отмены действия соответственно.

## Описание модуля `category`
Для класса Category определены следующие свойства:

* название (name),
* описание (description),
* список товаров категории (products).

Для класса Category есть строковое отображение в следующем виде:
*Название категории, количество продуктов: 200 шт.*

Для добавления товаров в категорию реализован специальный метод add_product() в классе Category, 
в который нужно передавать объект класса Product и уже его записывать в приватный атрибут списка товаров.

Также реализован геттер products, который выводит список товаров в виде строк в формате:
Название продукта, 80 руб. Остаток: 15 шт.

## Описание модуля `product_iterator`

Создан новый вспомогательный класс, с помощью которого можно перебирать товары одной категории, например в цикле for. 
Для этого новый класс принимает на вход объект класса категории и производит итерацию по товарам, 
которые хранятся в данной категории. 
То есть метод выполнения следующего шага итерации должен возвращать очередной товар категории.


## Описание модуля `utils`

Реализована подгрузка данных по категориями и товарам из файла JSON. С помощью функцию `read_json` можно прочитать файл,
который нужно скопировать в папку /data в формате json.
Функция `create_objects_from_json` позволяет создавать объекты классов.

---
## Тестирование функций
В проект добавлена возможность протестировать все созданные функции с использованием pytest.

Команда для запуска тестирования:
`pytest `

Команда для генерации отчета о покрытии:
`pytest --cov=src --cov-report=html`

Наш проект покрыт юнит-тестами Pytest with 100% coverage.

---
## Лицензия:

Этот проект лицензирован по [лицензии MIT](LICENSE).
